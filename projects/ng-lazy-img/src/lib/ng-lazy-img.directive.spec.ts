import { NgLazyImgDirective } from './ng-lazy-img.directive';

describe('NgLazyImgDirective', () => {
  it('should create an instance', () => {
    const directive = new NgLazyImgDirective();
    expect(directive).toBeTruthy();
  });
});
